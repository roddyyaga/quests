# This file is generated by dune, edit dune-project instead
opam-version: "2.0"
synopsis: "Python requests but it's OCaml"
depends: ["dune" "ssl" "lwt_ssl" "lwt" "cohttp" "cohttp-lwt-unix"]
build: [
  ["dune" "subst"] {pinned}
  [
    "dune"
    "build"
    "-p"
    name
    "-j"
    jobs
    "@install"
    "@runtest" {with-test}
    "@doc" {with-doc}
  ]
]
